on: [push]

jobs:
  test_merge_job:
    runs-on: ubuntu-latest
    name: A job to test the merge
    steps:
    - name: Merge two files
      id: merged
      uses: ajthinking/merge-json-files@v1.0.9
      with:
        additions: '{"also": "this"}'
        target: 'data1.json'
